
The following symbolic link:

  .ssh -> /home/interlock/.interlock-mnt/.ssh

is included to allow users to upload an authorized_keys file within the
encrypted partition, exposed through INTERLOCK and mounted on .interlock-mnt,
granting optional SSH access as the interlock user.
